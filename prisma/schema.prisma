datasource db {
  provider = "sqlite" // or "postgresql", "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

/**
 * 🧍 User → can have many Orders
 * 🧾 Order → belongs to one User
 * 🧺 Order → can include many Products
 * 🍞 Product → can belong to many Orders
 * OrderProduct → join table to store which products belong to which orders and their quantity
 */

// 🧩 Enum for order status
enum OrderStatus {
  PENDING
  PROCESSING
  PAID
  SHIPPED
  DELIVERED
  CANCELLED
}

model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  password  String
  role      String   @default("customer") // or "admin"
  createdAt DateTime @default(now())

  // 👇 Relation: 1 User has many Orders
  orders Order[]
}

model Product {
  id          Int     @id @default(autoincrement())
  name        String
  description String?
  price       Float
  image       String?

  // 👇 Relation: A Product can belong to many Orders (through OrderProduct)
  orderProducts OrderProduct[]
}

model Order {
  id        Int         @id @default(autoincrement())
  status    OrderStatus @default(PENDING)
  createdAt DateTime    @default(now())

  // 👇 Relation: Each Order belongs to one User
  userId Int
  user   User @relation(fields: [userId], references: [id])

  // 👇 Relation: Each Order can include many Products (through OrderProduct)
  orderProducts OrderProduct[]
}

// 🧩 Join table between Order and Product
model OrderProduct {
  id        Int @id @default(autoincrement())
  orderId   Int
  productId Int
  quantity  Int @default(1)

  // Relations
  order   Order   @relation(fields: [orderId], references: [id])
  product Product @relation(fields: [productId], references: [id])

  @@unique([orderId, productId]) // Prevent duplicate product entries per order
}
